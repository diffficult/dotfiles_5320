# Waybar Legacy

This directory stores Waybar artifacts that are intentionally kept for historical reference during cleanup work, but are not part of the maintained runtime.

Current contents:
- `scripts/debug_manager.py` — ad hoc diagnostic helper for the old module-manager wiring investigation.

Rule: do not point active modules, scripts, or deploy paths at files under `legacy/`.
